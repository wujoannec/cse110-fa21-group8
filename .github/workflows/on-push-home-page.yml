name: on-push-home-page
on:
  push:
    branches:
      - home-page
jobs:
  UI_test:
    name: UI Test (on push to [home-page] branch)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm ci
      - run: npm run home-page-test
  style_test:
    name: Coding Style Test (on push to [home-page] branch)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npx prettier --check .
